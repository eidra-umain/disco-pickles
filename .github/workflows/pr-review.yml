name: PR Review

on:
  pull_request:
    branches: [main]

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # TODO: Configure Claude PR review integration
      - name: Auto Review
        run: echo "TODO: Configure automated PR review"
